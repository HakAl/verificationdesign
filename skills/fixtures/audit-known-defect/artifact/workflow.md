# Inventory summary workflow

The same generating agent writes and reviews the summary in one context.
It reports done after its own review. No separate checker reads an inventory source.
The script illustrates this control flow; it does not call a model.
