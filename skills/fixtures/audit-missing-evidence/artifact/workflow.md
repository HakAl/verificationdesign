# Inventory deployment note

The deployed inventory summarizer is said to run nightly.
This folder contains no deployed code, run logs, verifier identity or check reports.
The audit scope concerns that deployed behavior, which these files cannot establish.
